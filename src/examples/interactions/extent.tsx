import * as React from "react";

// import {
//   control, Controls, custom, interaction, //name spaces
//   Interactions, layer, Layers,     //group
//   Map, Overlay, Overlays, Util    //objects
// } from "react-openlayers";

export class Extent extends React.Component<any, any> {
  public render() {
    return (
      <div className="todo">
        TODO: Coming Soon(PR would be highly appreciated)
      </div>
    );
  }
}